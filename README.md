

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. set the `MongodDB_API_Key`, `Port Number` and  `Database name`  in .env on backend folder
3. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
4. Run the app:
   `npm run dev`
